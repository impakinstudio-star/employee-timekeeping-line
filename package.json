{
  "name": "employee-timekeeping-line",
  "version": "1.0.0",
  "private": true,
  "description": "Employee timekeeping and scheduling app with LINE webhook support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
